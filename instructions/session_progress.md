# GOAL AI — プロジェクトダッシュボード
> このファイルが Code のセッション引き継ぎの唯一の情報源
> セッション開始時に必ず読む。セッション終了時に必ず更新する。
> 仕様参照: docs/goal_ai_project_v6_4.md / docs/goal_ai_reference_v2.md

---

## 5行サマリー
- **Version:** v3.11.7
- **Next:** C16自己検証ループの実装+A1再検証（前回「差分0件」報告したが実際は4件の差分残存）
- **Last done:** A1ソース変更+デプロイ完了。しかしC16視覚検証の差分検出が不正確（ふとし目視で4件差分発見）
- **Open issues:** Codeの自己差分検出が機能していない。DOMクエリのみでレンダリング結果を確認していない
- **Proposals:** 10件（提案ログ参照）

## 現在地
- **バージョン:** v3.11.7
- **チェーン:** C16自己検証ループ実装 → A1再検証 → A4修正 → A2-A5判断 → テスト配布
- **次のミッション:** C16自己検証ループ実装+A1再検証

## ミッションキュー（上から順に実行）

### C16自己検証ループ実装+A1再検証（ふとし承認済み 2026-03-25 実装その20）
> 目的: Codeが自分でmockupと実装の差分を正確に検出し、差分があれば自己修正→再デプロイ→再検証するループを構築する。A1で実証する。
> リスク: 🟡中
> 参照: development_rules.md C16（Code固有セクション — 自己検証ループの全手順）

**背景:**
前回A1完了報告で「差分0件」としたが、ふとしが目視確認した結果、以下の4件が残存していた:
1. 「こんにちは！」テキストが削除されていない
2. プロフィールヒント「もしよかったら私をデザインで…」が表示されている
3. 入力ボックスが画面最下部固定のまま（mockupではプリセット直下）
4. タスクボックスがシンプル表示のまま（mockupではrich構造）

**原因:** DOMクエリで要素有無をチェックしただけで、レンダリング結果の視覚的差分を検出していなかった。

**ミッション内容（具体的修正指示は含めない — Codeが自己検出すること）:**
1. development_rules.md C16「Code固有（自己検証ループ）」を読む
2. C16の手順に従い、mockupスクショと実装スクショを撮影
3. 自己差分検出を実行し、残存差分を全てリストアップ
4. 差分がある場合: 自己修正 → 再デプロイ → ステップ2に戻る
5. 差分0件になったら: `docs/mockups/screenshots/a1_compare.png`（横並び比較画像）を保存
6. 完了報告に: 検出した差分一覧 + 修正内容 + 最終検証結果（差分0件）+ スクショパスを記載

**成功基準:** ふとしがFinderで`a1_compare.png`を開き、mockupと実装が一致していることを確認できること

**mockup更新内容（実装を正としてmockup側を変更）:**
- ツールバーアイコン順を🕐→🔍に変更（現行の「通常」モードチップは削除）
- プリセットを実装の6件に変更（今日やること整理/目標の進捗を確認/モチベーションが出ない/新しいスキルを学びたい/ストレス解消法/自分の強みを知りたい）
- プリセット下にAI最適化バッジを追加（pillスタイル、0%表示）

**実装修正内容（mockupを正として実装側を変更）:**
- サマリーストリップ（#home-summary）を削除
- 「こんにちは！」挨拶テキスト+説明テキストを削除（AI最適化バッジのみ残す）
- 入力ボックスの位置をプリセット直下に移動（現在の画面最下部固定→mockup準拠）
- タスクボックスをmockup準拠のrich構造に修正（日付グループ・優先度ドット・重みバッジ・期限表示）

**注意:**
- 02a_home_prechat.htmlはフラグメント形式（<!DOCTYPE html>なし、CSS変数未定義）。_viewer.htmlでCSS変数を提供してレンダリング
- C16検証時はlocalhost:8765/_viewer.html?f=02a_home_prechat.htmlで表示

---

### 逆方向デザイン照合 ✅完了（2026-03-24）
> 目的: 実装にあってmockupにない要素を全てリストアップする。正方向照合（C14-A）では検出できなかった「追加要素」を発見するための照合。
> リスク: 🟢低（調査のみ。コード変更なし）
> 参照: docs/mockups/（正のデザイン）、development_rules.md C14-B（逆方向照合ルール）

**背景（なぜこの照合が必要か）:**
実装その19でふとしが実機で目視確認したところ、以下のような「mockupにない要素」が多数発見された:
- ホーム画面:「こんにちは！」挨拶セクション、AI最適化バッジ、会話中のタスクボックス＋「タスク画面→」
- 会話中: タスク化チップ、「↓最新へ」ボタン、メッセージアクションアイコン
- タスク画面: ビュータブの絵文字アイコン、ヘッダーのタイトル欠落
- フィードバック: テキスト短縮、AI名前表示の差異
- プロフィール: 表示ビュー→常時編集フォームの構造差
これらは全て、正方向照合（mockup→実装）では検出されない。C14-Bルールに従い逆方向で全画面を照合する。

**照合手順（各画面で実行）:**
1. 実装の該当画面のHTML（index.html静的要素 + JS動的生成要素）を読む
2. JS動的生成は関数の出力HTMLを読む（buildXxxHTML, renderXxx等）
3. 条件分岐がある場合は全状態のUI要素をリストアップ（会話前/会話中、データあり/なし等）
4. 各要素がdocs/mockups/の対応HTMLに存在するか確認
5. **mockupにない要素は全て報告する。** 追加経緯（ミッション名 or 初期実装）も記録

**レイアウト順序の検証（追加）:**
- 各画面のUI要素の並び順（上→下）をmockupと比較する
- 特にホーム画面の要素順序: mockupでは presets → input → sep → tasks だが実装では異なる可能性がある

**全画面リスト:**
- [x] #01 サイドバー
- [x] #02a ホーム会話前
- [x] #02b ホーム会話中
- [x] #03 ホーム全機能（アクションボタン等）
- [x] #04a タスク一覧
- [x] #04b タスク詳細
- [x] #05a-e GoalHub各ペイン
- [x] #06a-d 私をデザイン各タブ
- [x] #07 オンボーディング
- [x] #08a-d 解析/フィードバック/設定/プラン

**出力:** docs/design_reverse_audit.md に結果記載済み ✅
**修正はしない。報告のみ。** ふとしが各要素について「残す/削除する/mockup更新」を判断する。

---

### ホームレイアウト順序修正（ふとし承認済み 2026-03-24 実装その19）
> 目的: ホーム画面（会話前）の要素順序をmockupに合わせる
> リスク: 🟡中（レイアウト変更）
> 参照: docs/mockups/02a_home_prechat.html

**仕様:**
- mockupの順序: ヒーロー → プリセット → 入力ボックス → 区切り線 → タスクボックス
- 現在の実装: ヒーロー → プリセット → [挨拶セクション] → [区切り線] → [タスクボックス] → 入力ボックス（最下部固定）
- **入力ボックスの位置をmockupに合わせる**（タスクボックスの上に移動）
- 挨拶セクションの扱いは逆方向照合の結果を待って決定（このミッションでは触らない）

---

### 開発インフラ改善バッチ（ふとし承認済み 2026-03-24 実装その19）
> 目的: バージョンインクリメント漏れ+canopy肥大化+デプロイ後検証不在+session_progress肥大化+frontend-dist git管理の5つのプロセス設計漏れを一括修正
> リスク: 🟡中（インフラ変更。実装コードへの影響なし）
> 参照: development_rules.md C2/C10

**1. bump-version.sh 新規作成**
- `scripts/bump-version.sh patch|minor|major` で4箇所（globals.js, sw.js, index.html, worker.js）を一括インクリメント
- 設計判断はCodeに委任

**2. post-deploy-check.sh 新規作成**
- デプロイ後に本番の/api/versionを叩き、ローカルAPP_VERSIONと一致するか検証
- Pagesの/index.htmlからバージョン表示を確認
- 不一致ならFAIL（即rollback判断）
- C2基準フローのステップ6で使用

**3. pre-commit hook拡張**
- 現在の「3箇所一致チェック」に加え、「src/ または frontend/ に変更があるのにAPP_VERSIONが前commitから変わっていない」場合FAILを追加

**4. canopy.sh diff出力ネスト修正**
- canopy_latest.txtにはテスト結果本体のみを保存（diffセクションをstrip）
- diff出力は別ファイル or 表示のみ（保存しない）
- 設計判断はCodeに委任

**5. session_progress.md スリム化**
- 完了済みミッション記録をinstructions/results/に移動
- session_progress.mdには5行サマリー+現在地+ミッションキュー+提案ログのみ残す（200行以内目標）
- 照合結果は既にdocs/に配置済み（design_checklist_v2.md等）

**6. frontend-dist/ を.gitignore化**
- .gitignoreにfrontend-dist/を追加
- git rm --cached frontend-dist/ で追跡を解除
- canopy #20のbuild sync検証はvite build直後の実行で担保（gitではなくプロセスで保証）

**7. APP_VERSIONを3.11.4に一括更新（現在3.11.0のまま）**
- bump-version.sh作成後、最初のテストとして実行

**8. development_rules.md Cルール番号順並べ替え**
- 現在C16→C17→C18→C19→C20→C21→C15の順。C15をC14の直後に移動し、C15→C16→…→C21の番号順にする

**デプロイ戦略:** 1-4を一括実装→canopy→デプロイ→5-6は別デプロイ（git操作を含むため）→7は6完了後にbump-version.shで実行

---

## 提案ログ（2026-03-24 キュー空時自律調査）

以下はdocs/goal_ai_reference_v2.md バックログ + design_spec_v3.md + design_amendment_001.md から検出した未実装項目。コードベースgrepで不在を確認済み。

| # | 項目 | 仕様出典 | リスク | 備考 |
|---|------|----------|--------|------|
| 1 | チャット背景プリセット（夕焼け/星空等） | reference_v2 §Backlog | 🟡 | UI仕様あり、コード0件 |
| 2 | AIメモ自動更新（5ターンごと） | reference_v2 §Backlog | 🟡 | ai_memo_updated_atフィールドはあるがトリガーロジックなし |
| 3 | リファラル報酬の自動処理 | reference_v2 §Backlog | 🔴 | Stripe連携必要。完全未実装 |
| 4 | 過去会話からゴール候補を抽出 | reference_v2 §Backlog E-10 | 🟡 | ゴール検出は会話中のみ、履歴マイニングなし |
| 5 | 会話テーマ自動タグ付け | reference_v2 §Backlog E-20 | 🟢 | 完全未実装 |
| 6 | 複数ゴール同時検出 | reference_v2 §Backlog E-13 | 🟡 | 現在は1ターン1ゴールのみ |
| 7 | クイックゴール（長押し→即登録） | reference_v2 §Backlog E-16 | 🟢 | タスク長押しはあるがゴール用なし |
| 8 | 達成レポート検出+紙吹雪 | reference_v2 §Backlog E-17 | 🟢 | 紙吹雪パーティクルはあるが検出ロジックなし |
| 9 | 定期チェックイン（ストリーク連動） | reference_v2 §Backlog E-19 | 🟡 | ストリーク表示はあるがチェックインプロンプトなし |
| 10 | プロフィール理解セクション スクロール時自動閉じ | reference_v2 §Backlog | 🟢 | 未実装 |

**注:** A4プロフィール修正・A2-A5判断はClaude.aiからのキュー追加待ち。

## 完了済みミッション・照合結果・提案ログ過去分

→ **instructions/results/session_history.md** に移動済み（2026-03-24 開発インフラ改善バッチ）
