{
  "name": "app-frontend",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npx playwright test --project=mobile"
  },
  "dependencies": {
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "vite": "^8.0.0"
  }
}
