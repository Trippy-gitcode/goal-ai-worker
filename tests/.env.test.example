# GOAL AI — テスト用環境変数（プレースホルダ。実値は tests/.env.test に記載し、絶対に commit しない）
# 使い方: cp tests/.env.test.example tests/.env.test して値を設定
# tests/.env.test は .gitignore §5.1 で除外されている

WORKER_BASE=https://goal-ai-worker.goalai-futoshi.workers.dev
FRONTEND_BASE=https://goal-ai-frontend.pages.dev
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_REPLACE_ME
SUPABASE_ANON_KEY=sb_publishable_REPLACE_ME
STRIPE_TEST_KEY=sk_test_REPLACE_ME
