# GOAL AI — 開発ルール
> 更新: 2026-03-24（実装その18: C2 vite build必須化、C10 frontend-dist検証、C15 Vite tree-shake防止、canopy #20/#21追加）

---

## 方針

**品質維持を最優先に、Code が自律的に実装・テスト・検証する。ふとしの作業は承認判断のみ。**

---

## 役割分担（3層構造）

| 担当 | 責務 | 具体例 |
|------|------|--------|
| ふとし | 経営判断・承認 | 「何を作るか」の最終決定、リリース判断 |
| Claude.ai | 仕様・方針・コスト/プラン影響のある設計判断 | プラン構成、API選定、コスト試算、ルール策定 |
| Code | アーキテクチャ設計・実装設計・手順ルール管理・実装・テスト | 関数分割、モジュール構成、デプロイ手順、品質ゲート |

**Codeの設計権限の制約:** コスト構造またはプラン間の差別化に影響するアーキテクチャ変更は、提案ログに記載して承認を待つ。

**手順ルール（C2, C5, C7, C10）の管理権限:** Codeが自律的に追加・改善できる。ただし既存ルールの削除・緩和はClaude.ai承認が必要。

---

## Claude Code ルール（実装時）

### C1. 参照ドキュメントを全て読んでから実装開始
- CLAUDE.md の契約セクション・参照ドキュメントリストを確認
- 該当する指示書 + amendments を全て読む
- アドホック指示（テンプレ外の直接指示）でも同様。修正や調査に着手する前にCLAUDE.mdの参照ドキュメントリストを確認し、関連する指示書が存在する場合は必ず読んでから着手する
- 不明点は推測で埋めない。session_progress.md に記録して停止

### C2. 1機能1デプロイ・基準フロー
基準フロー（全ステップを省略しない）:
1. 実装
2. バージョンインクリメント — `scripts/bump-version.sh patch` で4箇所一括更新 [C10]
3. フロントエンド変更時: `npm run deploy:frontend`（vite build + pages deploy）[C15]
4. canopy.sh → PASS
5. デプロイ（Worker: `wrangler deploy`）
6. デプロイ後ヘルスチェック — `scripts/post-deploy-check.sh`（/api/versionのレスポンスがAPP_VERSIONと一致すること）
7. git tag v$(APP_VERSION) → git push → 次
- FAILなら wrangler rollback → ログ確認 → 修正 → 再デプロイ
- 複数機能をまとめてデプロイしない
- git tagはAPP_VERSIONから自動生成（番号体系を一本化）

**UI変更ミッションの追加ルール:**
- 上記ステップ1〜7に加え、ステップ6.5として **C16視覚検証** を実施する
- C16の手順をミッション定義の間接参照（「C16参照」）に留めず、完了チェックリストとして展開すること
- 完了チェックリスト（UI変更ミッション用テンプレート）:
  ```
  □ ソース変更（mockup更新 + 実装修正）
  □ bump-version.sh patch
  □ npm run deploy:frontend
  □ canopy.sh → PASS
  □ wrangler deploy
  □ post-deploy-check.sh → PASS
  □ C16視覚検証: localhost mockup配信 → 本番URLスクショ → 検証①②③ → 差分0件
  □ git tag → git push
  □ 完了報告に検証方法を明記（C20）: 「ソースレベル / デプロイ後本番URL」
  ```
- **「canopy PASS」だけで「完了」としない。** デプロイ+視覚検証まで完了して初めて「完了」

### C3. ファイル変更範囲の厳守
- ミッションに関係ないファイルは変更禁止
- 大規模編集後はgrepで保全確認
- diff を instructions/results/ に自動保存

### C4. ログ確認前の投機的修正禁止
- バグ → まず wrangler tail / DevTools でログ確認
- 原因特定 → 修正 → テストの順序を守る

### C5. verify.sh を自分で作成
- 新規追加した関数は全て grep 存在確認
- 既存関数の保全確認は累積（canopy.sh に追加）
- 判定は PASS/FAIL で明確に。曖昧な「たぶんOK」は禁止

### C6. 判断の線引き
- アーキテクチャ設計（全体構造・モジュール分割・API設計）: 自分で判断OK。根拠を記録
- 実装設計（関数内部・アルゴリズム選択）: 自分で判断OK。根拠を記録
- **コスト/プラン差別化に影響する設計:** 提案ログに記載して承認を待つ（例: API呼び出し回数変更、モデル振り分け変更、キャッシュ戦略変更）
- 仕様（何を作るか）: 変更禁止。CLAUDE.md 契約セクション参照
- ミッション遂行に必要なバグ修正: 許可（修正内容を記録+verify.shに検証追加）
- ミッションと無関係なバグ発見: 提案ログに報告するだけ。修正しない
- 改善案: session_progress.md 提案ログに記録するだけ。実装しない

### C7. 品質ゲート
- Phase 0: プリフライトチェック（前提条件の機械的検証。FAIL→停止）
- diff保存: git diff → instructions/results/
- grep保全: 新規+既存の全項目チェック
- キャノピーテスト: canopy.sh 実行（FAIL→rollback→停止）
- git tag: stepN-complete
- 結果レポート: session_progress.md に規約通り記録

### C8. キュー空時の自律提案
- ミッションキューが空になったら、docs/goal_ai_project_v6_4.md + docs/goal_ai_reference_v2.md + コードベースgrepで未実装タスクを特定
- session_progress.mdの提案ログに候補リストを記載して停止。実装はしない
- ふとし（またはClaude.ai）の承認後にのみキューに移動される

### C9. セッション開始時の参照ファイル検証
- CLAUDE.mdの参照ドキュメントリストに記載された全ファイルの存在をチェック
- 不在のファイルがあれば提案ログに報告して停止（そのまま実装開始しない）

### C10. デプロイ手順（バージョン同期）
- デプロイ前に `APP_VERSION` を必ずインクリメント
- **バージョンインクリメントはC2基準フローのステップ2で実行する。** 単独で記憶に頼らない
- `scripts/bump-version.sh patch` で4箇所を一括更新（手動同期禁止）
- 同期が必要な4箇所: globals.js, sw.js, index.html, worker.js
- sw.js の `CACHE_NAME` を `'goal-ai-v' + APP_VERSION` に更新
- バージョン番号ルール: 大変更=整数、小変更=小数第一、極小=小数第二
- git tagはAPP_VERSIONから自動生成: `git tag v$(APP_VERSION)`（C2ステップ7）
- **frontend-dist/index.htmlのバージョンもソースと一致すること。** canopy.sh #20で自動検証される
- canopy.sh #12 でバージョン同期を自動検証する。canopy FAILならデプロイしない
- pre-commit hookで「src/ または frontend/ に変更があるのにAPP_VERSIONが前commitから変わっていない」場合FAILにする
- **このルールはCodeが自律管理する。** 対象ファイルが変わった場合はCodeが更新する

### C11. バグ対応フロー
ふとしがバグ報告した場合:
- UI/実装バグ（明らかなもの）: ふとしから直接受けて即修正。記録必須
- 仕様判断が必要: session_progress.mdに記録して停止。Claude.ai経由で判断を待つ
- コスト/プラン影響あり: 同上

Codeがバグを発見した場合:
- ミッション遂行に必要: C6に従い即修正（記録必須）
- コスト/プラン影響あり: 提案ログに記載→承認を待つ
- その他: 提案ログに記載。キュー空時に自律修正可
- **横展開チェック必須:** バグ修正完了後、同類のバグが他の箇所にないか横展開チェックを実施する。結果をsession_progress.mdに記録

**既知のバグパターン集（修正時に照合すること）:**

| # | パターン | 原因 | 対策 |
|---|---------|------|------|
| 1 | 大規模編集で過去修正消失 | 一括上書き | grep保全チェック |
| 2 | 投機的修正 | ログ未確認 | ログ→原因→修正 |
| 3 | 二重バブル | クリーンアップ不完全 | AbortController完全版 |
| 4 | CSS/DOM不一致 | 不在セレクタ | grep確認後に記載 |
| 5 | API全停止(400) | ヘッダー無効 | デプロイ後スモークテスト |
| 6 | バージョン未更新 | C2基準フロー未遵守 | bump-version.sh + pre-commit hook |
| 6 | 二重ルーティング | 旧パス残存 | 旧パス削除明記 |
| 7 | 環境変数スコープ | ローカル定義 | export/importパス明示 |
| 8 | SWキャッシュ古い | CACHE_NAME未更新 | インクリメント |
| 9 | content空 | reasoning tokens消費 | maxTokens 300以上 |
| 10 | 二重実行 | JS+プロンプト両方 | 単一責任 |
| 11 | UI変更がgrep PASSでもレンダリング未反映 | 関数存在≠呼び出し | HTMLレンダリングパスを追跡 |

### C12. インラインスタイル禁止 + UI変更時のJS検査
**R1. インラインスタイル禁止ルール:**
- JSの`innerHTML`や`.style.cssText`で色・角丸・ボーダーを直書きしない
- 全てCSSクラスまたはCSS変数（`var(--xxx)`）で指定する
- 新規コードでハードコード色（`#xxx`, `rgba(...)`)をインラインスタイルに追加しない
- 例外: PDF/PPTX出力（スタンドアロンHTML）、テーマプレビュー（リテラル色表示）、JS計算値（動的alpha等）

**R2. UI変更時のJS検査必須化:**
- UI/デザイン変更時は、HTML/CSSだけでなくJSファイルのインラインスタイルも必ず検査する
- 検査コマンド: `grep -rn 'border-radius\|border:\|background:\|color:' frontend/js/ | grep -v '\.bak'`
- canopy.shにスタイル整合性チェックを含める
- **このルールはCodeが自律管理する。** 追加OK、削除・緩和はClaude.ai承認必要

### C13. ミッション完了時の必須アクション
ミッション（キューのステップ）完了時に以下を必ず実行:
1. verify.sh / canopy.sh → 全PASS
2. session_progress.md → 完了記録を書く
3. git tag → バージョンタグ付与
4. git push → リモートに反映
5. **UI変更を含むミッションの場合:** 視覚検証結果をレポートに含める（C16参照）。視覚検証不可の場合は「ふとし確認待ち」と明記し、次のUI変更ミッションに進まない
6. **完了レポートに検証範囲を明示する（C20）。** 何を検証したか、何を検証していないかを書く。確認状態（自律判定/ふとし確認済み）も記載
- 1つでも欠けた状態で次のミッションに着手しない
- **このルールはCodeが自律管理する。**

### C14. デザイン照合の方法論（mockup vs 実装）
> **mockup原本完全性ルール:** docs/mockups/のHTMLファイルはデザインの唯一の正（Single Source of Truth）。
> Code・Claude.aiの両方において、mockup HTMLを手書きで再構成・要約・部分抽出することを禁止する。
> mockupの表示には必ず元HTMLファイルをブラウザでそのままレンダリングする（localhost配信 or file://）。
> 理由: 手書き再構成は要素順序の入れ替え、SVGアイコンの絵文字化、CSS詳細の省略を引き起こし、照合の基準自体が不正確になる。
デザイン照合タスクでは「機能が存在するか」ではなく「mockup通りに実装されているか」を判定する:

**A. 正方向照合（mockup → 実装）:**
- mockup HTMLの要素を1つずつ読み、タグ名・クラス名・テキスト・CSS値・アイコン種別を記録
- 実装コードの対応要素を特定し、上記を項目ごとに比較
- ❌の判定基準: タグ名が違う、テキストが違う、CSS値が±2px以上ずれている、アイコンがemoji↔SVGで違う、子要素構成が異なる
- 「grepで関数が存在する→✅」は不可。実際のHTML構造を読んで比較すること

**B. 逆方向照合（実装 → mockup）:**
- 実装画面に表示される全UI要素（HTML静的要素 + JS動的生成要素）をリストアップする
- 各要素がmockupに存在するか確認する
- **mockupにない要素は全て報告する。** 「後から追加された機能だから正当」と自己判断しない
- JS動的生成（buildXxxHTML(), renderXxx()等）も照合対象。ソースのgrepだけでなく、関数の出力HTMLを読むこと
- 報告形式: `[画面名] 要素名 — mockupになし / 追加経緯（ミッション名 or 初期実装）`

**C. 「スケーリング差」の扱い:**
- mockupが340px固定フレームであることを理由に、実装値の方が正しいと自己判断しない
- スケーリング差と判断する場合でも、乖離として報告し、ふとしまたはClaude.aiの承認を得ること

- **このルールはCodeが自律管理する。** 追加OK、削除・緩和はClaude.ai承認必要

### C15. Vite ESモジュール環境での関数グローバル露出
Vite ESモジュール環境では、関数はモジュールスコープに閉じ込められる。HTML onclickやJS動的innerHTML内のonclickから呼ばれる関数は、Object.assign(window, {...})に登録しないとビルド時にtree-shakeされて消える。
- **新しい関数をHTML onclickまたはJS動的onclickから呼ぶ場合、必ず該当ファイル末尾のObject.assign(window, {...})に追加すること**
- 関数を定義しただけではグローバルに露出しない。window登録がないとビルド後にボタンが動かなくなる
- canopy.sh #21で自動検証される。FAILなら修正してから再デプロイ
- **このルールはCodeが自律管理する。** 追加OK、削除・緩和はClaude.ai承認必要

### C16. デザイン検証フロー（完了条件）
デザイン関連ミッションの「完了」には、ソースコード比較だけでなくブラウザレンダリング検証が必須:

**検証手順（Code・Claude.ai共通）:**
1. **mockup配信:** `cd docs/mockups && python3 -m http.server 8765` でlocalhostサーバーを起動
2. **mockup表示:** ブラウザで `http://localhost:8765/{画面}.html` を開く（元HTMLをそのままレンダリング。手書き再構成禁止 — C14参照）
3. **実装表示:** ブラウザで本番URL `https://goal-ai-frontend.pages.dev/` を開き、JSでターゲット画面に遷移（例: `showPage('myself');switchMyselfTab('profile');`）
4. **横並び比較:** 比較用HTMLページ `_compare_{画面ID}.html` を作成し、左iframe=mockup（localhost）、右iframe=実装（本番URL）で横並び表示。同一オリジン制約がある場合は別タブで並べてスクショ
5. **スクショ撮影:** 横並び状態のスクリーンショットを撮影（スマホビューポート: 375-390px幅）
6. **事前チェック①:** mockup側が元HTMLと同一であることを目視確認。要素順序・SVGアイコン・CSS値が一致しない場合は配信方法を見直す
7. **事前チェック②:** 実装側が正しい画面・状態を表示しているか確認（遷移完了・ローディング完了・サイドバー閉じ）
8. **事前チェック③:** mockupと実装の差分を全てリストアップし、各差分を分類（意図的追加/実装ミス/mockup未更新/要判断）
9. **ふとし提示:** チェック①②③全てOKの場合のみ、スクショ＋差分テーブルをふとしに提示し承認を得る

**禁止事項:**
- mockup HTMLを手書きで再構成してビジュアライザーやiframeに注入すること
- ソースコードの差分比較だけで「見た目が同じ」と判定すること
- mockupのレンダリング結果を確認せずにふとしに判断を求めること

**Claude.ai固有:**
- Claude in Chromeが利用可能な場合、DCでlocalhostサーバー起動→Chromeで両方を開いてスクショ撮影→チャットに表示
- Chrome未接続の場合、ふとしに実装スクショの送付を依頼。mockupは元HTMLファイルを共有（present_files）

**Code固有（デザイン実装後の自己検証ループ）:**
Codeがデザイン実装ミッション完了時、以下の自己検証ループを実行する。差分0件になるまで完了報告しない。

**検証手順:**
1. localhostでmockup HTMLを配信し、ブラウザ（Playwright推奨）でスクショ撮影
2. デプロイ後の本番URLをブラウザで開き、該当画面に遷移してスクショ撮影
3. **自己差分検出:** mockupスクショと実装スクショを視覚的に比較し、全差分をリストアップ。以下の**全10ファクター**で検証すること（1つでも省略しない）:
   1. **要素の有無** — mockupにある要素が実装にない、またはその逆
   2. **要素の順序** — 上→下の並び順がmockupと一致するか
   3. **テキスト内容** — ラベル・プレースホルダー・ボタン文言がmockupと完全一致するか
   4. **スペーシング（margin/padding/gap）** — 要素間の余白がmockupと同等か。目視で明らかに広すぎる/狭すぎる間隔は差分
   5. **flex配分（flex:1の割当先）** — どの要素が残空間を吸収するか。mockupで詰まっているのに実装で広がっていれば差分
   6. **要素のY座標位置** — 画面内の絶対的な位置がmockupと大幅にずれていないか。スクショを横並びにして同じ要素の高さが揃っているか確認
   7. **要素サイズ（font-size/width/height）** — アイコン・ボタン・テキストのサイズがmockupと一致するか
   8. **色・透明度** — 背景色・文字色・ボーダー色・opacity がmockupと一致するか
   9. **border/border-radius** — 枠線の太さ・角丸がmockupと一致するか
   10. **ビューポート幅によるレスポンシブ差** — mockupは340px固定フレーム。実装のスクショ幅がモバイル（375-390px）であること。幅の違いによるレイアウト崩れがないか
   - 「データがない状態」を「差分なし」と混同しない（例: タスクボックスがrich構造だがデータが空なのと、そもそもrich構造が実装されていないのは別）
   - 「cookie/セッション制御で非表示」を「削除済み」と混同しない
4. **差分あり → 自己修正:** 差分を修正 → 再デプロイ → ステップ1に戻る
5. **差分0件 → スクショ保存:** `docs/mockups/screenshots/{画面ID}_mockup.png`, `{画面ID}_impl.png`, `{画面ID}_compare.png`（横並び比較画像）を保存
6. **完了報告:** スクショのファイルパス + 「チェック①OK / ②OK / ③差分0件」+ 検証方法（C20）を明記

**横並び比較画像（_compare.png）の作成:**
- mockupとimplを横並び1枚にした画像を生成する（Playwrightまたは画像処理ライブラリ）
- ふとしがFinderで1枚の画像を開くだけで比較できるようにする

**自己差分検出の精度ルール:**
- DOMクエリ（querySelector等）による要素有無チェックだけでは不十分。レンダリング結果の視覚的な差分を検出すること
- 「ソースコードを変更した = 画面に反映された」と仮定しない。デプロイ後の本番URLで実際に表示されている状態を確認する
- 差分を「データ依存」「cookie制御」等で正当化する場合、その根拠をコードレベルで示すこと（関数名+条件分岐を明記）

- ふとしはFinderで `docs/mockups/screenshots/` を開いて `{画面ID}_compare.png` を確認するだけで最終承認できる
- Claude.aiはDCの `view` でスクショを確認し、Codeの検証結果を検証する（Chrome操作不要）
- **このルールはCodeが自律管理する。** 追加OK、削除・緩和はClaude.ai承認必要

### C17. UI変更時のmockup同時更新
UI変更を含むミッションでは、実装と同時にdocs/mockups/の該当HTMLファイルを更新すること:
- mockupは「現在あるべき姿」を常に反映する。凍結された過去の状態ではない
- 実装で要素を追加/削除/変更した場合、mockupにも同じ変更を反映する
- mockup更新後、正方向照合（C14-A）でmockupと実装が一致することを確認
- **mockupを更新せずにUI変更を「完了」にしない**
- **このルールはCodeが自律管理する。** 追加OK、削除・緩和はClaude.ai承認必要

### C18. UI変更はビジュアルチェック合格済みmockup必須
UI変更を含むミッションをキューに置く前に、ふとしのビジュアルチェックが必須:
- **Claude.ai起点:** このチャットでmockup作成/更新 → ふとしが目視OK → キューに追加
- **Code起点（提案ログ）:** UI変更ありの提案はCodeが実装しない。提案ログに記載→Claude.aiチャットに上げる→mockup作成→ふとしOK→キュー
- **ふとし起点:** このチャットで議論→mockup作成→ふとしOK→キュー
- 提案ログテンプレートに「UI変更あり/なし」列を含めること。「UI変更あり」の場合、Codeは承認されても自律実装しない
- **例外なし。全てのUI変更はふとしのビジュアルチェックを通る**
- **このルールはCodeが自律管理する。** 追加OK、削除・緩和はClaude.ai承認必要

### C19. 人間的ミス表現の禁止と構造的原因報告
「忘れた」「間違えた」「見落とした」「うっかり」等の人間的ミス表現を使わない。AIにうっかりは存在しない。
エラー/乖離が発生した場合、以下のテンプレートで報告する:

```
事象: [何が起きたか — 事実のみ]
分類: ルール不在 / 検証不在 / 参照フロー欠如 / コンテキスト制約 / 指示の曖昧さ / 仕様未定義
根拠: [なぜその分類か]
再発防止: [機械的ゲートの提案]
```

- 「一致」「完了」「問題なし」と判定する場合、検証方法と結果を明記する
- 検証していない項目を「確認済み」と書かない
- 「既に実装済み」と判断する場合、grepで関数存在を確認するだけでは不可。機能が動作する画面遷移パスが存在することを確認する
- **このルールはCodeが自律管理する。** 追加OK、削除・緩和はClaude.ai承認必要

### C20. 完了報告の検証範囲明示
ミッション完了報告時に「何を検証したか」と「何を検証していないか」を明示する:
- 例: `canopy PASS（ソース静的チェック22項目。デプロイ後の本番検証は未実施）`
- 例: `照合完了（正方向C14-Aのみ。逆方向C14-Bは未実施）`
- 完了記録に確認状態を記載: `自律判定` or `ふとし確認済み`
- **このルールはCodeが自律管理する。** 追加OK、削除・緩和はClaude.ai承認必要

### C21. 再発防止策の導入時テスト
再発防止策（canopy項目追加、pre-commit hook、スクリプト等）を導入した場合:
- 導入直後に「わざと問題を起こして防止策が検出するか」のテストを1回行う
- テストがFAILを検出 → 防止策が機能している → 元に戻して次へ
- テストがPASSしてしまう → 防止策が機能していない → 修正してから次へ
- **このルールはCodeが自律管理する。** 追加OK、削除・緩和はClaude.ai承認必要

---

## Claude.ai ルール（設計・方針決定時）

### A1. 誤り発生時の横展開・再発防止
- 間違い・誤解・意図しない事象が発生した場合:
  1. 類似ケースが他にないか横展開チェック
  2. 再発防止策を提案
- 「このケースだけ」で終わらせない。パターンとして捉える

### A2. 5セッションごとの棚卸し
- 5セッション（このチャットの会話数）ごとに以下を確認:
  1. CLAUDE.md・development_rules.md・session_progress.mdの整合性
  2. docs/のナレッジファイルが最新か
  3. 参照ドキュメントリストに不在ファイルがないか
  4. 提案ログに未処理の項目がないか
  5. mockup vs 実装の累積差分（5件以上の乖離がある場合、mockup更新または要素削除の判断をふとしに依頼）
  6. 直近5セッションで「忘れた/間違えた/見落とした」等の人間的ミス表現が使われていないかチェック。使われていた場合、該当箇所を特定し構造的原因の再分析を行う
- 不整合があれば修正提案を出す

### A3. 変更時の一括更新義務
- 仕様・方針・ルールの変更が発生した場合、ふとしの承認を得た上で関連する**全ファイル**を一括更新する
- 更新対象の特定: 変更内容に応じて CLAUDE.md / development_rules.md / session_progress.md / docs/ / bootstrap.md / メモリ の影響範囲を洗い出す
- 「1箇所だけ更新して他を放置」は禁止。不整合の原因になる
- 更新完了後、更新したファイル一覧をふとしに報告する

### A4. セッション終了時の成果物配置チェック
- このチャットで仕様書・指示書・デザインドキュメントを出力した場合:
  1. リポジトリの`docs/`または`instructions/`に配置されたか確認
  2. CLAUDE.mdの参照ドキュメントリストに含まれているか確認
  3. 配置漏れがあればDC経由で即配置する
- 「Codeに渡す必要がない」と「リポジトリに配置する必要がない」を混同しない
- プロジェクトナレッジの差し替え指示だけでなく、**リポジトリへの配置**を必ず確認する

### A5. 改善提案（毎セッション）
- セッション内で議論が完了したトピックに対して、6視点から優先度付きで最低3つ提案:
  🤝営業 / 🔧エンジニア / 📋PM / 🏗️アーキテクト / 💻SE / 👤エンドユーザー
- 競合差別化提案を1〜3つ別枠で提示

### A6. デザインドキュメント更新フロー
- amendment作成 → Claude.aiがDC経由でdesign_spec_v3.md本文に統合 → amendmentに「統合済み」マーク
- **タイミング:** amendment作成直後（後回しにしない）
- design_spec_v3.mdが常に最新の確定仕様（Single Source of Truth）

### A7. Code完了報告の検証義務（Claude.ai側）
Codeから「完了」報告を受けた時、Claude.aiは以下を確認してからふとしに報告する:
- 報告内容がミッション定義の全要件をカバーしているか
- 「全完了」の根拠が明示されているか（検証方法+結果）
- 未確認事項を「確認済み」と表現していないか
- C20（検証範囲明示）が守られているか
- 確認できない場合は「Codeの報告を転記。Claude.ai側では未検証」と明示する

### A8. デザインビジュアルチェックフロー（Claude.ai側）
デザインの変更前後をふとしに提示して承認を得るフロー。mockup HTMLが唯一のオリジナル。

**STEP 1: Codeに差分調査ミッションを渡す（Claude.ai）**
- session_progress.mdに「デザイン差分調査: {画面ID}」ミッションを追加（ふとし承諾後）
- Codeが実行: localhostでmockup配信→ブラウザでスクショ撮影→事前検証①②③→差分リスト+スクショをレポート出力

**STEP 2: Codeのレポートを読んで提示を準備（Claude.ai）**
- Codeが出力した差分レポートを読む
- 各差分に推奨判断を付記（mockup準拠 / mockup更新 / テスト配布後FB）

**STEP 3: ふとしに差分を提示（Claude.ai）**
- Codeが撮影したスクショをふとしに表示（横並び or 順次）
- チャット上で差分をテーブル形式で説明:
  | # | 項目 | mockup | 実装 | 分類 | 推奨判断 |
- ふとしに項目ごとの判断を求める

**STEP 4: 判断結果のCodeへの引き渡し（Claude.ai）**
- ふとしの判断が確定したら、ミッション定義としてsession_progress.mdに書き込む（ふとしの書き込み承諾後）
- mockupの更新・実装の修正は全てCodeが実行。Claude.aiはHTMLファイルを作成・編集しない（鉄則2・7）

**絶対禁止:**
- Claude.aiがmockup HTMLファイルを作成・編集すること（鉄則2・7違反）
- mockup HTMLを読んで推測してビジュアライザーで再構成すること
- Codeの検証レポートなしにふとしに判断を求めること

### A9. デザイン設計フロー（新規・変更共通）

**フロー1: 新規デザイン**
1. 要件定義（ふとし + Claude.ai）→ チャットで議論
2. デザイン案作成（Claude.ai）→ ビジュアライザーで提示
3. フィードバック → 承認までイテレーション
4. mockup HTML化（Code）→ docs/mockups/に保存
5. 実装（Code）→ C16検証フロー実行
6. 視覚検証（Code → ふとし）→ 最終確認

**フロー2: 既存デザイン変更**
1. 変更起点の特定（ふとし or Claude.ai or Code）
2. 差分特定（Code）→ localhostでmockup配信→スクショ→検証①②③→レポート出力
3. 差分提示（Claude.ai）→ Codeのレポートを読み、推奨判断付きでふとしに提示
4. 判断（ふとし）→ 各差分の方針を決定
5. ミッション定義（Claude.ai）→ 判断結果を整理（ファイル書き込みはふとし承諾後）
6. mockup更新 + 実装修正（Code）→ C16検証フロー（差分0件確認）
7. 視覚検証（Code → ふとし）→ 最終確認

**役割分担:**
- ふとし: 要件提示、ビジュアル承認、判断決定、書き込み承諾
- Claude.ai: デザイン案作成（ビジュアライザー）、差分分析・提示、ミッション定義。HTML編集はしない
- Code: mockup HTML作成・更新、実装、C16検証フロー、差分調査レポート
